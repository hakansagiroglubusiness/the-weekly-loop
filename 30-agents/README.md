# Agents

Subagent definitions for delegated analysis.

Use an agent (rather than a skill) when the work needs its own context window:
broad exploration, parallel analysis across many accounts, or an adversarial
second opinion on a recommendation.

## Planned

| Agent | Purpose | Week |
|---|---|---|
| `audit-verifier` | Adversarially checks a `paid-audit` recommendation before it reaches a human | 7 |
